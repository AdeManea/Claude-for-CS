---
name: discount-threshold-monitoring
description: "Flags deals where discount exceeds the approved threshold for that segment. Routes approval to the correct authority based on discount depth using thresholds from company profile. Tracks discount freq"
argument-hint: ""
---

Read the skill file at `skills/discount-threshold-monitoring/SKILL.md` (relative to this plugin's installed directory, i.e. `~/.claude/plugins/rev-ops/skills/discount-threshold-monitoring/SKILL.md`), then execute it with the following arguments: $ARGUMENTS
